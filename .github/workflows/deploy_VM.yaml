name: Gitops Openstack Selfhosted VM

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Rend deploy_alpine.sh executable
        run: chmod +x scripts/deploy_alpine.sh

      - name: Déploiement de la VM alpine et du serveur web nginx
        run: bash scripts/deploy_alpine.sh test1

